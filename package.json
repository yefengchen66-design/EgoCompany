{
  "name": "ai-company",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @ai-company/server dev & pnpm --filter @ai-company/client dev",
    "dev:server": "pnpm --filter @ai-company/server dev",
    "dev:client": "pnpm --filter @ai-company/client dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild",
      "node-pty"
    ]
  }
}
